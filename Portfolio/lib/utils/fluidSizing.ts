/**
 * Re-export del sistema de sizing fluido desde shared
 * Mantiene compatibilidad con imports existentes en Portfolio
 */

export { fluidSizing, clamp, fluidStyle } from '../../../shared/fluidSizing';
