/**
 * Alert helpers para Admin
 * Re-exporta el sistema de alertas compartido
 */

export { alerts, alertManager } from '@/shared/alertSystem';
export type { Alert, AlertType, AlertPosition, AlertConfig } from '@/shared/alertSystem';
